# 2026年2月 学習記録

### 学んだこと
- 
- 

### つまずいたポイント

### 疑問点
- ××について理解が浅い



### 学んだこと
- 新規作成と編集を見分けるには
新規作成 : @comment = Comment.new → id がない
編集 : @comment = Comment.find(params[:id]) → id がある
- N+1問題
 @boards = Board.all:全ての掲示板を取得している.
　　掲示板の数が１０個あれば、１０回SQLを実行する。
その後に .includes(:user) をつけると
@boards = Board.all.includes(:user)
全ての掲示板を取得して、さらにユーザー情報も一緒に取得している
- ransackを使って投稿の検索機能を実装。MVCを設定。
コントローラの設定:
class ProductsController < ApplicationController
  def index
    @q = Product.ransack(params[:q])
    @products = @q.result
  end
end
ビュー
<%= search_form_for @q do |f| %>
  <%= f.label :name_cont, '商品名' %>
  <%= f.search_field :name_cont %>

  <%= f.submit '検索する' %>
<% end %>

<% @products.each do |product| %>
  <p><%= product.name %></p>
<% end %>
Articlesモデルを編集する
索させたいカラムと関連するモデルを定義する必要がある
app/controllers/articles_controller.rb
class ArticlesController < ApplicationController
  def search
    @q = Article.ransack(params[:q])
    @articles = @q.result(**distinct: true**).includes(:user).page(params[:page]).per(20)
  end
end
distinct: trueは、Ransackの検索結果に対して重複したレコードを削除する機能
- resourcesを宣言するだけで、コントローラのindex、show、new、edit、create、update、destroyアクションを個別に宣言しなくても1行で宣言が完了する

- resources :photosというルーティングをルーティングファイルに追加すると、コントローラやビューで以下の_pathヘルパーが使えるようになる
| 返すURL　         | _pathヘルパー | 
|------　　　       |---------|
| /photos          | photos_path |
| /photos/new      | new_photo_path |
| /photos/:id/edit | edit_photo_path(:id) |
| /photos/:id      | photo_path(:id) |

上記の:idなどのパスヘルパーのパラメーターは、生成されたURLに渡される。つまり、edit_photo_path(10)は/photos/10/editを返す。

- データ操作系 → 複数形
　機能・画面系 → 単数形

| 複数形　         | _単数形 | 
|------　　　       |---------|
| 複数のデータを扱う     | 単一の概念を扱う |
| ユーザー一覧、投稿一覧など）|（ホーム画面、ログイン機能など） |
| 特定のモデル（データの種類）を扱う | 特定のページや機能を表す |
| データベースを変更するアクションを持つ| データの作成・更新・削除を行わない |
| （create, update, destroy） |1つのデータを扱うとき、
詳細表示（show アクション）、編集（edit アクション）、
新規作成（new アクション） |
|例: UsersController | 例: HomeController |
|PostsController| DashboardController |
|ProductsController | SessionController |

- redirect_toメソッドの基本的な役割と動作の仕組み
コントローラーでredirect_toが実行される
Railsは指定されたURLと共に300番台のHTTPステータスコード（通常は302）をレスポンスとして返す
ブラウザは新しいURLに対して改めてリクエストを送信する
新しいアクションが実行され、結果が表示される

|特徴           | redirect_to	        | render |
|------　       |---------              |---------|
|HTTPリクエスト数 | 2回（リダイレクト要求 + 新規リクエスト | 1回 |
|URL変更         | あり                   |  なし|
|インスタンス変数 | 新規リクエストで再設定が必要 | 継続して使用可 |
|使用シーン  | ・フォーム送信後の画面遷移 | ・バリデーションエラー時 |
|          | ・ログイン要求 | ・検索結果表示 |
|          | ・処理完了後の遷移 | ・部分的な画面更新 |


- page(...)->order(...) （SQLレベルでの並べ替えにならない）
@bookmark_boards = @q.result(distinct: true).includes(:user).order(created_at: :desc).page(params[:page])

- 



### つまずいたポイント
-  N+1問題の理解が薄い
- パスヘルパーメソッドの違いがよくわかっていないため、エラーがよく起きていた
- 単数形、複数形をあまり考えていなった。
- page(...)->order(...) 順番がよくわかっていなかった。

### 疑問点
- ××について理解が浅い

## 参考リンク
- 【【Rails】N+1問題とは？ includesメソッドで解決する】https://qiita.com/ryota21/items/62c6613410907a478e9e
- [Rails]ransackによる検索　16/20
https://zenn.dev/redheadchloe/articles/f33b6745fb57ea
- Railsガイド　Rails のルーティング
https://railsguides.jp/routing.html
- Railsのコントローラー名：複数形？単数形？迷ったときの判断方法
https://qiita.com/G-nenjo/items/48ca104bb765b46f464c
- Rails初心者必見！redirect_toの全知識と9つの実践テクニック【解説付き】
https://dexall.co.jp/articles/?p=1468
- 
- 