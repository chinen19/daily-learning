## published_at
### 基本
published_at は
* 記事を「いつ公開するか」を決める日時
* 過去の日時なら「すでに公開できる状態」
* 未来の日時なら「まだ公開できない、公開待ちの状態」

### 例
例1: 過去の日時が設定されている場合
@article.published_at = "2024-01-01 10:00"  # 過去の日時
* すでに公開日時が過ぎているので、「公開」できる状態
* ステータスは「公開」になる
* 
例2: 未来の日時が設定されている場合
@article.published_at = "2025-12-31 10:00"  # 未来の日時
* まだ公開日時が来ていないので、「公開待ち」の状態
* ステータスは「公開待ち」になる
* 
例3: published_at が空の場合
@article.published_at = nil  # 空
* 公開日時が設定されていないので、「公開する」ボタンを押した時に現在時刻が設定される
* 現在時刻は過去ではなく「今」なので、ステータスは「公開」になる


## adjust_state
### 基本
adjust_state メソッドは、published_at の日時を見て、ステータスを自動的に調整するメソッド
公開日時が「未来の日付」の場合 → ステータスを「公開待ち」に変更
公開日時が「過去の日付」の場合 → ステータスを「公開」に変更

### 例
def adjust_state
  if published_at が未来の日時
    ステータスを「公開待ち」にする
  elsif published_at が過去の日時
    ステータスを「公開」にする
  end
end


【Rails】FatControllerをスッキリさせる
https://study-diary.hatenadiary.jp/entry/2020/10/08/201732


### 「更新する」ボタンを押した時の流れ
def update
  authorize(@article)
  
  # 1. フォームから送られてきた情報を @article に設定する
  @article.assign_attributes(article_params)
  
  # 2. adjust_state メソッドで、published_at の日時に応じてステータスを調整する
  @article.adjust_state unless @article.draft?
  
  # 3. 保存する
  @article.save!
  
  flash[:notice] = '更新しました'
  redirect_to edit_admin_article_path(@article.uuid)
end


# binding.pry
### 基本
プログラムの作業を行う際に、バグの有無、エラーの原因箇所、コード上の問題がないかなどを確認するために用いるツールのこと

### 使用方法
以下の様に、処理の確認をしたいコードの箇所に
binding.pryを書く

#### コード
    def create
      User.create(user_params)
      binding.pry
      redirect_to root_path
    end
end

binding.pryの記述している箇所で処理が止まる

   3: def create
   4:   User.create(user_params)
=> 5:   binding.pry
   6: redirect_to root_path
   7:  end

  [1] pry(#<TweetsController>)>

#### 例
投稿フォームに入力した内容が保存されないという問題を解決したい。
実際にparamsで取り出せるパラメーターの中に入力した情報があるかを確かめたい。

  [1] pry(#<TweetsController>)>params

##### 結果
paramsメソッドが実行され、返り値がターミナルに出力。
[1] pry(#<TweetsController>)>params
=>いろんな表示があり{“name” =>””, “job_id” =>”1”}
paramsの結果を確認することができた。 よくみるとnameのバリューが""と何も入っていないことがわかった。


デバックツール(pry-rails)について　binding.pryの使い方
https://qiita.com/STHEXA/items/1de2411cb3987066c9b9

【Rails】 Pryを使ってデバッグをしてみよう！
https://pikawaka.com/rails/pry


## FactoryBot 

### 1. FactoryBot とは
- テスト用データを簡単に作成するライブラリ
- メリット: 
　FactoryBot を使うと コードが短くなる
　同じデータを何度も作る時に便利
　テストデータの管理が楽になる


### 2. trait の役割
- 同じモデルで違う状態のデータを作る機能
- 使い方: 
　　# trait を使わない場合
　　future_article = create(:article, state: :published, published_at: 　　　1.hour.from_now)
　　past_article = create(:article, state: :published, published_at: 1.day.ago)

　　# trait を使う場合
　　future_article = create(:article, :future)
　　past_article = create(:article, :past)


### 3. create と build の違い
- create: ファクトリに基づいたオブジェクトを生成し、データベースにも保存する
user = create(:user)
- build: 指定したファクトリに基づいたオブジェクトを生成するが、データベースには保存されない
# factory_botを利用しない場合
user = User.new(nickname: "TEST", email: "test@example.com")

# factory_botを利用する場合
user = build(:user)
- 使い分け: データベースの保存。高速化したいバリデーションテストにはbuildが適している。

### 4. 今回の問題解決で学んだこと
- 問題: 未来公開の記事を使ったテストが失敗していた。
  記事のステータスが「published_request」になるはずが、 「draft」のままだった。
- 原因: :future トレイトに state がなかった
- 解決方法: `:future` トレイトに `state { :published }` を追加した。
- 学び: なぜテストが通らないのか、辿れなかった。トレイト、state が全くわかっていなかった。MVCの流れを意識できていなかった

### 参照
FactoryBotの基本的な使い方
https://qiita.com/ishikawa_slj/items/95249f07039eb51db313



## 「npx・yarn」それぞれの特徴

### 「npm・yarn」はJavaScriptのパッケージに関わる仕組み

### JavaScriptのパッケージマネージャーとは
-　パッケージを実行、インストール、削除など、管理する仕組みのこと

### 問題
- プレビュー画面で不要なリンクが表示されていた

### npmの特徴
-　どの開発環境でも一貫した操作が可能
インストールが遅い
パッケージの整合性チェックなどはない


### Yarnの特徴
-　npmと互換性がある
インストールが速い
ライブラリの整合性チェックを厳密に行うため、セキュリティが高い
強固なバージョン管理が可能
　 npmと互換性があるため、npmと同様のコマンドが使用可能

### 困ってたこと
パッケージ、npx・yarnなどを理解していなかったため、インストールができなかった。ls -la を実行して、どのロックファイルがあるか確認が必要だとわかった。

### 参考文献
【初心者でもわかる！】「npm・npx・yarn」それぞれの特徴まとめ
https://iret.media/97888

JavaScriptのパッケージマネージャーnpmとYarnについて解説します！
https://www.sejuku.net/blog/96866


## メソッドとは
オブジェクト指向プログラミングにおいて、オブジェクトに対する操作を定義した手続き

### メソッドの定義
メソッドを定義するには、実行する処理内容をdef ～ end 内に記述。
def メソッド名(引数)  # 引数は任意
  # 実行する処理内容
end

### メソッドの実例
def communication(name)②
 "Hello, #{name}"  # 戻り値③ ④
end
puts communication('Taro')  # 'Taro'を引数として渡す①

【詳細】 ①puts communication('Taro’)：引数を作成 ②def communication(name)~ end：メソッドを設定 ③引数('Taro') ⇒(name)へ入る ④"Hello, #{name}" 流れ：('Taro') ⇒(name)⇒"Hello, #{name}" ⇒Hello,Taro
：メソッドで定義された動作を行う戻り値を設定

### 投稿フォームなどを使用する場合
  def create
    @book = Book.new(book_params)
    @book.save
      redirect_to book_path(@book)
  end

  private
  # ストロングパラメータ

  def book_params
    params.require(:book).permit(:title, :body)
  end

### privateストロングパラメータとは
ストロングパラメーターとは、
指定したキーを持つパラメーターのみを受け取るように制限するもの。
意図しないデータの更新をされる可能性を防ぐため。
※今回permit(:title, :body)に絞った。

### book_paramsの定義
  def create　←  フォームで入力されたデータを受け取っている
    @book = Book.new(book_params)
    @book.save
      redirect_to book_path(@book)
  end

  private
  # ストロングパラメータ

  def book_params
    params.require(:book).permit(:title, :body)
  end

↑book_paramsはフォームで入力されたデータを受け取っている。
paramsはRailsで送られてきた値を受け取るためのメソッド。
requireでデータのオブジェクト名(ここでは:book)を指定し、
permitでキー（:title,:body）を指定。
これにより、book_paramsの中にフォームで入力されたデータが格納される。

###　参考文献
Rails メソッドを定義する（引数・戻り値・ストロングパラメーター の設定方法）
https://qiita.com/acekame/items/6d40def54dd7b6a1dcd1


### 「関数」と「メソッド」の違い

### オブジェクト指向とオブジェクト、インスタンス
オブジェクト指向：「モノ（どんなやつで、どう動く）」に注目した考え方
オブジェクト：物。データのまとまり
インスタンス：具体的な「1つのオブジェクト」

### 関数とメソッド
関数：処理のまとまり。何かを入れると何かを計算して何かを返してくれるやつ
メソッド：オブジェクト指向における「操作」を定義したもの

オブジェクト：犬

インスタンス1：ポチ
- プロパティ：
  - 名前 = "ポチ"
  - 年齢 = 3
  - 体重 = 10kg
  - 犬種 = "柴犬"
  - 毛の色 = "茶色"
- メソッド：
  - 吠える() → "ワンワン！"
  - 走る() → 公園を3周する
  - 座る() → おすわりをする

インスタンス2：ハチ
- プロパティ：
  - 名前 = "ハチ"
  - 年齢 = 5
  - 体重 = 25kg
  - 犬種 = "ゴールデンレトリバー"
  - 毛の色 = "金色"
- メソッド：
  - 吠える() → "ワウワウ！"
  - 走る() → ボールを追いかける
  - 寝る() → ソファーで寝る

###参考文献
 「分かりそう」で「分からない」でも「分かった」気になれるIT用語辞典
https://wa3.i-3-i.info/diff97function.html


オブジェクト？インスタンス？？プロパティ？？？メソッドーーー！
https://zenn.dev/funxxfun/articles/4faf8436afde4b


### users_pathとuser_pathの基本的な役割
Railsではresources :usersといった形でルーティングを設定していることを前提
* users_path
    * ユーザーの「一覧ページ」のURLを生成します
    * 例: /users
    * 主にindexアクションに対応しています。
* user_path(@user)
    * 特定のユーザーの「詳細ページ」のURLを生成します。
    * 例: /users/1
    * 主にshowアクションに対応しています。


### 正しい使い分け
ユーザー一覧ページへのリンクを生成する場合
users_path

特定のユーザーの詳細ページへのリンクを生成する場合
user_path(@user)

### 困っていること
ルーティングの理解が薄く、リンク生成の時に頭に出てこないので、意識しないといけない。

### 参考文献
Railsルーティングの基本：users_pathとuser_pathの違い
https://zenn.dev/take_tech/articles/14dc30c40a2b2a


### デベロッパーツールのNetworkタブ
画像がうまく表示されない、ボタンを押してもデータが送られていない等の問題を確認できるツール

###  Network タブの基本画面
* Name: リクエストされたファイルの名前やURLの一部が表示
* Status: HTTPステータスコード。通信が成功したか、失敗したかなどを示す。
* Type: リクエストされたリソースの種類（document, stylesheet, script, image, Workspace, xhr など）。
* Initiator: このリクエストが開始されたきっかけ（例: HTMLパーサーがCSSファイルを要求、JavaScriptがAPIを呼び出し）。
* Size: ファイルのサイズ。10 KB / 100 KB のように表示されている場合、10 KB が実際に転送されたサイズ（圧縮されている場合など）、100 KB が元のファイルサイズを示す。
* Time: リクエストが開始されてから完了するまでの時間。
* Waterfall: 各リクエストのタイミングを視覚的に示したグラフ。リクエストがどのくらいの時間、何に待たされていたか（キューイング、DNS検索、接続、SSL、リクエスト送信、サーバーの応答待ち、ダウンロード）を分析できます。ボトルネック（処理が遅い原因）を見つけるのに役立つ

### その他
キャッシュを無効化
「Disable cache」というチェックボックスをオンにすると、ブラウザのキャッシュ（一度読み込んだファイルを一時的に保存しておく機能）が無効になる。
* 使い所: CSSやJavaScriptを変更したのに表示が変わらない場合、キャッシュが原因かも。これをオンにしてリロードすれば、必ずサーバーから最新のファイルを取得しにいくため、変更が反映されているか確認できる。
* また、初めてページを訪れたユーザーの読み込み速度を測定するのにも役立つ。
* 注意点: この機能は、デベロッパーツールが開いている間だけ 有効。

### 参考文献
簡単にNetworkタブについてまとめてみた
https://zenn.dev/noah11/articles/4059fefddf7312



###  slimの書き方
slimには閉じタグという概念がない
代わりにインデントを使い、マークアップする範囲を指定する。

### 概要
<>不要 <%= %> → = <% %> → - コメント → / id指定 → # class指定 → .

### コード例
テキストの書き方
テキストは|で定義
p
  |　ピカわか

属性の書き方
タグ内の属性は下記のように記述。 aタグの例）
a href='http://www.pikawaka.com' ピカわか

IDとclassの書き方 divは省略することができるので書いても書かなくても良い。
#pikawaka
  ピカわか

div.pikawaka
  ピカわか

.pikawaka
  ピカわか

複数定義する場合
#pikawaka.hoge.fuga
  ピカわか

.pikawaka.hoge
  ピカわか

JavaScriptの書き方
:javascript
  コード

link_toのとき
slim→html
= link_to "テキスト", "パス", data: { 属性名: '値' } 

# 例
= link_to "TOP", root_path, data: {target: "hoge"}

# 下記のようにコンパイル
<a data-target="hoge" href="/">TOP</a>

render yield
new.html.slim
/ 誤
= render 'form', user: @user

/ 正 （render yield は ==）
== render 'form', user: @user

if文
- if current_user.nil?
  li 新規登録
- else
  li ログイン

/ end不要


### 参考文献
【Rails】 slimの書き方をマスターしよう！
https://pikawaka.com/rails/slim

【初心者向け】slimの書き方
https://qiita.com/wann/items/2b8151fc39c201cfa889