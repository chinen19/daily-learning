## published_at
### 基本
published_at は
* 記事を「いつ公開するか」を決める日時
* 過去の日時なら「すでに公開できる状態」
* 未来の日時なら「まだ公開できない、公開待ちの状態」

### 例
例1: 過去の日時が設定されている場合
@article.published_at = "2024-01-01 10:00"  # 過去の日時
* すでに公開日時が過ぎているので、「公開」できる状態
* ステータスは「公開」になる
* 
例2: 未来の日時が設定されている場合
@article.published_at = "2025-12-31 10:00"  # 未来の日時
* まだ公開日時が来ていないので、「公開待ち」の状態
* ステータスは「公開待ち」になる
* 
例3: published_at が空の場合
@article.published_at = nil  # 空
* 公開日時が設定されていないので、「公開する」ボタンを押した時に現在時刻が設定される
* 現在時刻は過去ではなく「今」なので、ステータスは「公開」になる


## adjust_state
### 基本
adjust_state メソッドは、published_at の日時を見て、ステータスを自動的に調整するメソッド
公開日時が「未来の日付」の場合 → ステータスを「公開待ち」に変更
公開日時が「過去の日付」の場合 → ステータスを「公開」に変更

### 例
def adjust_state
  if published_at が未来の日時
    ステータスを「公開待ち」にする
  elsif published_at が過去の日時
    ステータスを「公開」にする
  end
end


【Rails】FatControllerをスッキリさせる
https://study-diary.hatenadiary.jp/entry/2020/10/08/201732


### 「更新する」ボタンを押した時の流れ
def update
  authorize(@article)
  
  # 1. フォームから送られてきた情報を @article に設定する
  @article.assign_attributes(article_params)
  
  # 2. adjust_state メソッドで、published_at の日時に応じてステータスを調整する
  @article.adjust_state unless @article.draft?
  
  # 3. 保存する
  @article.save!
  
  flash[:notice] = '更新しました'
  redirect_to edit_admin_article_path(@article.uuid)
end


# binding.pry
### 基本
プログラムの作業を行う際に、バグの有無、エラーの原因箇所、コード上の問題がないかなどを確認するために用いるツールのこと

### 使用方法
以下の様に、処理の確認をしたいコードの箇所に
binding.pryを書く

#### コード
    def create
      User.create(user_params)
      binding.pry
      redirect_to root_path
    end
end

binding.pryの記述している箇所で処理が止まる

   3: def create
   4:   User.create(user_params)
=> 5:   binding.pry
   6: redirect_to root_path
   7:  end

  [1] pry(#<TweetsController>)>

#### 例
投稿フォームに入力した内容が保存されないという問題を解決したい。
実際にparamsで取り出せるパラメーターの中に入力した情報があるかを確かめたい。

  [1] pry(#<TweetsController>)>params

##### 結果
paramsメソッドが実行され、返り値がターミナルに出力。
[1] pry(#<TweetsController>)>params
=>いろんな表示があり{“name” =>””, “job_id” =>”1”}
paramsの結果を確認することができた。 よくみるとnameのバリューが""と何も入っていないことがわかった。


デバックツール(pry-rails)について　binding.pryの使い方
https://qiita.com/STHEXA/items/1de2411cb3987066c9b9

【Rails】 Pryを使ってデバッグをしてみよう！
https://pikawaka.com/rails/pry


## FactoryBot 

### 1. FactoryBot とは
- テスト用データを簡単に作成するライブラリ
- メリット: 
　FactoryBot を使うと コードが短くなる
　同じデータを何度も作る時に便利
　テストデータの管理が楽になる


### 2. trait の役割
- 同じモデルで違う状態のデータを作る機能
- 使い方: 
　　# trait を使わない場合
　　future_article = create(:article, state: :published, published_at: 　　　1.hour.from_now)
　　past_article = create(:article, state: :published, published_at: 1.day.ago)

　　# trait を使う場合
　　future_article = create(:article, :future)
　　past_article = create(:article, :past)


### 3. create と build の違い
- create: ファクトリに基づいたオブジェクトを生成し、データベースにも保存する
user = create(:user)
- build: 指定したファクトリに基づいたオブジェクトを生成するが、データベースには保存されない
# factory_botを利用しない場合
user = User.new(nickname: "TEST", email: "test@example.com")

# factory_botを利用する場合
user = build(:user)
- 使い分け: データベースの保存。高速化したいバリデーションテストにはbuildが適している。

### 4. 今回の問題解決で学んだこと
- 問題: 未来公開の記事を使ったテストが失敗していた。
  記事のステータスが「published_request」になるはずが、 「draft」のままだった。
- 原因: :future トレイトに state がなかった
- 解決方法: `:future` トレイトに `state { :published }` を追加した。
- 学び: なぜテストが通らないのか、辿れなかった。トレイト、state が全くわかっていなかった。MVCの流れを意識できていなかった

### 参照
FactoryBotの基本的な使い方
https://qiita.com/ishikawa_slj/items/95249f07039eb51db313
