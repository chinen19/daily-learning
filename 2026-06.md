## cat -e と sed コマンドの役割の違い
### cat -e の役割
cat -e ファイル名
このコマンドは:
* 各行の 末尾に $ を表示 
* 改行コードが正しく認識されているか を確認できる
例:
.box-body$
  - if embed.identifier?$
$ が表示される = 改行が正しく認識されている

### sed 's/ /·/g' の役割
cat ファイル名 | sed 's/ /·/g'
このコマンドは:
* スペースを · (中点)に変換 して表示
* インデントが何文字分か が一目でわかる
例:
.box-body
··-·if·embed.identifier?
····-·if·embed.youtube?
· の数を数える = インデントのスペース数がわかる


# ER図レビュー振り返り

## 問題
**指摘内容：**
- Memosテーブルの `category_id` に対するリレーション線がない
- カーディナリティが未記載
- Categories と Users が不適切に紐付いている

## 理由
リレーション線が必要な理由
- 外部キーがあれば、必ずリレーション線を描く

### カーディナリティが必要な理由
- 1対多か多対多かで設計が変わる
- データの流れを明確にするため

## Categories と Users を紐付けてはいけない理由
- Categories は固定カテゴリ（マスターデータ）
- ユーザーがカテゴリ作成できるなら紐付ける
- ユーザーごとに変わらない共通データ
- 正しい関係：Users → Memos → Categories


### 修正内容
1. **Memos と Categories のリレーション線を追加**
   - Memos（N）─ Categories（1）

2. **カーディナリティを明記**
   - 外部キーを持つ側が「多（N）」
   - 1つのカテゴリに複数のメモが属する

3. **Categories と Users のリレーションを削除**
   - 固定カテゴリなので直接紐付かない

## 学んだこと
### ER図作成の基本ルール
- 外部キーがある = リレーション線を描く
- カーディナリティを必ず明記
- マスターデータとユーザーデータを区別


## デプロイエラーの対応

### 問題 
- Renderデプロイ時、必要なボタンが表示されずDB作成で詰まった。
- A Iのの古い情報と、現在のRenderの仕様が乖離していた。

### 解決法 
-  1. 画面のURLを直接ブックマークし、推測ではなく確定した手順で実施。
    2. AIに対し「フリープランで可能な範囲か」「どの画面のどのボタンか」と具体的かつ制約を明示して質問。
    3. `Gemfile`と環境変数（ENV）の整合性をログを見ながら確認。
- 結果： 無事にデプロイ完了。

###  学び 
- 教訓: 仕様変更は「当たり前」と捉え、情報源を「教材のみ」にせず「公式ドキュメント」や「ブラウザのログ」と突き合わせる重要性を学んだ。


 ## layouts/_footer.html.erbとshared/_footer.html.erbの違い
### 配置場所
app/views/layouts/ _footer.html.erb
app/views/shared/_footer.html.erb

### 用途
layouts/_footer.html.erbは、レイアウト専用の部品なので、レイアウトファイルからのみ 呼び出す
shared/_footer.html.erbは、複数のビューで共有する部品なので、どこからでも呼び出せる

### 呼び出し方
render 'layouts/footer'
render 'shared/footer'
フッターのパーシャルファイルは 1つあれば十分 。


## Category.fixed と Category.all の違い

### 取得するカテゴリ
Category.allはすべてのカテゴリ
Category.fixedは固定カテゴリのみ (fixed: true)

### ヘッダーの表示
Category.allはすべて表示される
Category.fixedは主要なカテゴリだけ表示

### 表示場所の変更
Category.allはビューファイルを編集
Category.fixedはデータベースで管理

### 使い分け
Category.allは
* 管理画面 でカテゴリの一覧を表示する場合
* サイドバー ですべてのカテゴリを表示する場合
* フッター ですべてのカテゴリを表示する場合

Category.fixedは
* ヘッダー で主要なカテゴリだけを表示する場合
* トップページ で特集カテゴリを表示する場合
* モバイルメニュー で重要なカテゴリだけを表示する場合


### パフォーマンス
Category.allはデータ量が多い
Category.fixedはデータ量が少ない


