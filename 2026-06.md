## cat -e と sed コマンドの役割の違い
### cat -e の役割
cat -e ファイル名
このコマンドは:
* 各行の 末尾に $ を表示 
* 改行コードが正しく認識されているか を確認できる
例:
.box-body$
  - if embed.identifier?$
$ が表示される = 改行が正しく認識されている

### sed 's/ /·/g' の役割
cat ファイル名 | sed 's/ /·/g'
このコマンドは:
* スペースを · (中点)に変換 して表示
* インデントが何文字分か が一目でわかる
例:
.box-body
··-·if·embed.identifier?
····-·if·embed.youtube?
· の数を数える = インデントのスペース数がわかる