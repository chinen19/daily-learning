## Ruby
Rubyには、ターミナルに文字を出力するためメソッドとして、puts、print、pなどのメソッドがある。
* putsメソッドは、引数を文字列として出力。返り値はnil。
* pメソッドは、引数をそのまま出力。返り値は引数そのもの。

引数
引数は、関数に渡す値。
def multiply(x, y)
  return x * y
end

puts multiply(2, 3) # 6 と出力されます。
戻り値
関数が処理を終えた後に返す値。returnキーワードを使って戻り値を指定。


## JavaScript
Webページをインタラクティブに動作させる

### JavaScriptをWebページに組み込む方法

1．HTMLファイル内に記述
<script>
    alert('Hello, JavaScript!');
  </script>

2．JavaScriptコードを外部ファイルに保存し、HTMLファイルからそのファイルを読み込む
 <script src="script.js"></script>

HTMLと同じディレクトリに「script.js」というファイルを作成し、以下のコードを記述。
// script.js
alert('Hello, JavaScript!');

3．HTML要素の属性としてJavaScriptコードを記述し、特定のイベント（クリック、マウスオーバーなど）に反応して実行する
<body>
  <h1>JavaScriptの呼び出し方法</h1>
  <button onclick="alert('Hello, JavaScript!')">Click me!</button>
</body>


### 基本的な文法

1. 変数の宣言
var、let、constを使い
2. データ型
* 数値（Number）
* 文字列（String）
* 真偽値（Boolean）
* オブジェクト（Object）
* 配列（Array）：順序付けられたデータの集合。
* 未定義（Undefined）
* Null

3. 演算子
let a = 5;
let b = 2;
console.log(a + b); // 足し算: 7
console.log(x == y); // 等しいか: false

4. 制御構文
if文
switch文
forループ

### 関数
特定の処理をまとめて再利用できるようにするためのもの

// 関数の定義
function greet(name) {
    return "Hello, " + name + "!";
}

// 関数の呼び出し
console.log(greet("Alice")); // "Hello, Alice!" と出力されます。
* function：関数を定義するキーワード
* greet：関数の名前
* name：関数の引数（パラメータ）で、関数に渡す値を受け取る
* return：関数の結果を呼び出し元に返す

#### アロー関数
アロー関数は、簡略化された関数の書き方。
const greet = (name) => "Hello, " + name + "!";
console.log(greet("Charlie")); // "Hello, Charlie!" と出力されます。
* =>：アロー関数の記号。左辺に引数、右辺に処理
* {}：処理が1行の場合は省略できる
* return：処理が1行の場合は省略できる
* ()：引数が1つの場合は省略できる。


##要素の取得
DOMの要素を取得するためのメソッドを使う
getElementById：IDを指定して要素を取得
getElementsByClassName：クラス名を指定して要素を取得
getElementsByTagName：タグ名を指定して要素を取得
querySelector：CSSセレクタを使って要素を取得
addEventListener：様々なイベント処理を実行

### addEventListenerを分解
button.addEventListener('click', changeText); は、次の3つの部品でできている。
* button → 誰に対して仕掛けるか（今回はボタン）
* 'click' → どんな出来事を待つか（今回はクリック）
* changeText → その出来事が起きたら何をするか（今回はchangeText関数を実行）
日本語にすると
「button さんに、click という出来事が起きたら、changeText を実行してもらう」というお願いを1行で書いているイメージ。

### 気づき
JavaScriptの文法で変数を使っている認識がなかった。
アプリ制作のER図で
DOMの要素を取得するためのメソッドの内容覚えていなくて苦戦したが、要素を分解したら、わかりやすかった


## RSpec

### RSpecとは
RSpecは、テストを実行するためのテストフレームワークの一つ。
Railsアプリでは、RSpecを使ってモデル、ビュー、コントローラーなどのコンポーネントをテストすることが一般的。

### 単体テスト（モデルスペック）と結合テスト（システムスペック）

#### 単体テスト（モデルスペック）
クラスやメソッドが正しく動作するかテスト。 例：
* タイトルの文字数制限を超えたときにエラーがでるか
* 正しいユーザー名とパスワードでログインできるか

#### 結合テスト（システムスペック）
複数の単体テストの組み合わせをテスト。
機能テスト、E2Eテストと呼ばれることもある。単体テストの次に行われる。
コントローラー、モデル、共通メソッドなどの組合せをテストする。 例：
* カテゴリー一覧から特定のカテゴリーページに遷移できるか
* ユーザーがログインし、記事を投稿できるか


## E2E(End to End)テスト
E2Eテスト（End to Endテスト）は、ソフトウェア開発において、アプリケーションの全体的な動作を確認するための重要なテスト手法

#### E2E(System)テストと統合テストの違い
- E2Eテストはユーザーシナリオに沿った確認を重視。
- システムテストは要件との整合性チェックを重視する



#### テストをする理由
1. バグの早期発見 コードを自動的にテストし、バグの早期発見をすることで、本番環境に影響を与える前に修正できる。
2. コードの可読性向上 テストコードはコード全体の理解に役立つドキュメンテーションとしての役割にもなる。テストはコードがどのように動作するかを具体的に示すものなので、コードの利用方法や動きが文書化されたものとして利用できる。
3. 品質管理 テストを通じて客観的なデータとして品質を担保できるため、クライアントやユーザーからの信頼を築くことができる。


### 参照
[Rails]RSpecのセットアップから基本の書き方について
https://zenn.dev/redheadchloe/articles/20c3375b328e09

【Rails】RSpecとは - ユーザーモデルの単体テストをやってみる -
https://zenn.dev/ganmo3/articles/15c4be288b21db

RSpecテスト②コード解説(FactoryBot,Faker,let,Capybara)
https://zenn.dev/eliri/articles/8cd1e2829725e5

【Rails】はじめてのRSpec！テストコードを書こう！
https://techpit.intloop.com/courses/25/curriculums/26/sections/220/parts/775

E2Eテストとは？4つの観点と実施する目的・自動化ツールをご紹介
https://www.qbook.jp/column/2001.html

E2Eテストとは？メリット・デメリットについて解説！
https://eg-testing.co.jp/blog/test/post-blog-42/




### RSpecの基本形（用語と構造）
* テストケースを整理・分類する
    * describe...テストの対象機能の説明文
    * context...条件の説明文
* テストコードを実行する
    * it...アウトプットを記述
    * expect...（エクスペクテーション）期待される処理を記述する


### 参照
Rspecでのテスト入門~E2E(System)テスト基本文法編~
https://qiita.com/yosuke_takeuchi/items/915fc3092ee88bed3ff2



## FactoryBot

### FactoryBotとは
FactoryBotは、テスト用のデータを簡単に作成するためのライブラリです。テストケースごとにデータを手動で作成するのは面倒ですが、FactoryBotを使うことで、再利用可能なテンプレートを使って効率的にデータを生成できます。

### 参照
FactoryBotの基本的な使い方
https://qiita.com/ishikawa_slj/items/95249f07039eb51db313

Factorybotを使ったテストデータの作成方法
https://qiita.com/naoshi7770/items/38e7796c683e785ccf3a


## Capybara
### Capybaraとは
* capybara は、Ruby用のウェブアプリケーションテスト用のgem
* 主にRailsなどのウェブアプリケーションの自動化テストを行う際に利用される

### 役割
* ウェブアプリケーションのテストを簡単に記述するためのDSL（ドメイン固有言語）を提供する
* ブラウザ操作（クリック、フォーム入力、ページ遷移など）をシミュレートする
* 様々なドライバ（Selenium, Rack::Test, WebKitなど）をサポートし、ブラウザの動作をエミュレートする
* JavaScriptを含む動的なコンテンツのテストにも対応

### 使用する場面
* E2Eテスト（End-to-End Test）や受け入れテストで、アプリケーション全体のユーザー体験を検証する際
    * 例: ユーザーがログインして記事を投稿する一連のフローをテスト
* JavaScriptを利用した動的ページのテスト
* フォームのバリデーションやエラーメッセージの確認


### 参照
【初心者】Capybaraさん
https://zenn.dev/tatsuki_otake/articles/bc82c38d50f1e7




## bundle install と yarn installの違いについて
・bundle install → installするライブラリがgem
・yarn install → installするライブラリがJavaScriptについてのライブラリ

### bundle installとは
ライブラリをインストールするコマンドで、インストールする者がアプリ内で使えるかどうかをチェックしている。

### yarn installとは
yarnはパッケージマネージャーであり、gemのbunderのようなもの。
javascriptに関係するパッケージ（ライブラリをまとめたもの）のバージョンを管理して、ライブラリ同士でアプリ内で使用できるかをチェックしている。

### 参照
bundle install と yarn installの違いについて
https://qiita.com/narimiya/items/5132c7bfef8bb2d81685


### 気づき
カリキュラムやっている時は必死で気付かなかったけど、今振り返ると、 FactoryBotやCapybaraなど色々便利なGemを使っていたのかとわかった。
テストも段階分けているため、わかりやすく、バグがあっても見つけやすい仕組みだとわかった。
