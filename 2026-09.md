## Ruby
Rubyには、ターミナルに文字を出力するためメソッドとして、puts、print、pなどのメソッドがある。
* putsメソッドは、引数を文字列として出力。返り値はnil。
* pメソッドは、引数をそのまま出力。返り値は引数そのもの。

引数
引数は、関数に渡す値。
def multiply(x, y)
  return x * y
end

puts multiply(2, 3) # 6 と出力されます。
戻り値
関数が処理を終えた後に返す値。returnキーワードを使って戻り値を指定。
